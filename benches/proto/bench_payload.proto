syntax = "proto3";

package org.eclipse.uprotocol.bench;

message BenchPayload {
  uint32 case_id = 1;
  uint32 sequence = 2;
  uint32 logical_payload_len = 3;
  uint32 checksum = 4;
  bytes payload = 5;
}
